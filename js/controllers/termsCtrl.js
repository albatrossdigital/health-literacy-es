'use strict';

angular.module('app')

.controller('termsCtrl',
  
  ['$scope', 
    function ($scope) {

      //$scope.termData = appTermsFactory.getTermsFlow();
      //console.log($scope.termData);

    }
  ]
);